services:
  sarca:
    container_name: sarca
    image: ghcr.io/insigmo/sarca:latest
    volumes:
      - ./sarca.conf:/sarca.conf:ro
      - sarca-work-volume:/work
      - telegram-bot-api-data:/var/lib/telegram-bot-api
    environment:
      PORT: ${PORT:-8001}
      DATABASE_HOST: db
      WORK_DIR: /work
    ports:
      - "${PORT:-8001}:${PORT:-8001}"
    env_file:
      - sarca.conf
    restart: unless-stopped
    depends_on:
      - db
      - telegram-bot-api
    networks:
      - sarca-network

  telegram-bot-api:
    container_name: telegram-bot-api
    image: aiogram/telegram-bot-api:latest
    volumes:
      - ./sarca.conf:/sarca.conf:ro
      - ./docker/telegram-bot-api-entrypoint.sh:/sarca-telegram-entrypoint.sh:ro
      - telegram-bot-api-data:/var/lib/telegram-bot-api
    env_file:
      - sarca.conf
    environment:
      # Official Local Bot API flag (sarca.conf uses TELEGRAM_LOCAL_API for the app).
      TELEGRAM_LOCAL: "1"
    entrypoint: ["/bin/sh", "/sarca-telegram-entrypoint.sh"]
    restart: unless-stopped
    ports:
      - "8081:8081"
    networks:
      - sarca-network

  db:
    container_name: sarca_db
    image: postgres:15.0-alpine
    # Keep in sync with DATABASE_* in sarca.conf (example defaults).
    environment:
      POSTGRES_USER: sarca
      POSTGRES_PASSWORD: sarca
      POSTGRES_DB: sarca
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready --username=sarca --dbname=sarca"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - sarca-db-volume:/var/lib/postgresql/data
    networks:
      - sarca-network
volumes:
  sarca-db-volume:
    name: sarca-db-volume
  sarca-work-volume:
    name: sarca-work-volume
  telegram-bot-api-data:
    name: telegram-bot-api-data

networks:
  sarca-network:
    name: sarca-network
