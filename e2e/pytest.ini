[pytest]
pythonpath = .
addopts = -ra --strict-markers -m "not smoke"
markers =
    smoke: live Telegram upload smoke (needs storage workers; not run in CI)
    mock_only: needs the fake Telegram Bot API
    slow: takes more than a few seconds
