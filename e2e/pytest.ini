[pytest]
pythonpath = .
addopts = -ra --strict-markers
markers =
    smoke: media upload smoke (mock Bot API by default; real Telegram on demand)
    mock_only: needs the fake Telegram Bot API
    gui: drives the desktop client through tauri-pilot (needs a display)
    slow: takes more than a few seconds
